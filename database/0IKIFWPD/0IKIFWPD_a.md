A *subspace* of a vector space $V$ over a division ring $k$ is a subset $W \subset V$ that is itself a vector space under addition and scalar multiplication in $W$, i.e., it is closed under the two operations.
