An *open ball* in a metric space $(X, d)$ is a subset of the form

$$
B(x_0, r) = \\{ x \in X \\; : \\; d(x, x_0) < r \\}
$$

where $x_0$ is called the *center*, and $r > 0$ the *radius*, of the open ball.
