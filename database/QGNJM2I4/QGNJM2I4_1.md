Let $M$ be a metric space with distance function $d: M \times M \to \mathbb{R}_{\geq 0}$. Then the subsets

$$
B(x, r) := \\{ y \in M \\; : \\; d(y, x) < r \\}
$$

for all $x \in M$ and $r > 0$ generate a topology on $M$.
